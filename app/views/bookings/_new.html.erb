<div class= "booking-form">
   <%= simple_form_for [@profile, @booking] do |f| %>
    <%= f.input :start_date, as: :date, html5: true %>
    <%= f.input :end_date, as: :date, html5: true %>
    <div class= "bookingcosts">
      <p> <%= @profile.name%>'s day rate is £<%= @profile.day_rate%>
      <div class="total">
     <%# <h3> Total cost: <%= @profile.day_rate * (end_date%>
     </div>
   </div>
   <%# = f.submit "Submit booking",  %>
   <%= f.submit "Book #{(@profile.name).split(" ").first}?", class: "btn btn-primary" %>
  <%end%>
</div>
